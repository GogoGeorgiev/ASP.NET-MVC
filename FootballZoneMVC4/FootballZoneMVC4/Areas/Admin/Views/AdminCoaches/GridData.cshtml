@model IEnumerable<FootballZoneMVC4.DAL.Coach>

@foreach (var item in Model) {
    <tr data-pkey="@item.CoachID">
        <td>
            @Html.DisplayFor(modelItem => item.CoachNames)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Biography)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ImageUrl)
        </td>
        <td class="rowControl hidden">
            <a href="#makeRowEditable" class="makeRowEditable">Edit</a> |
            <a href="#deleteRow" class="deleteRow">Delete</a>
        </td>
    </tr>
}
