@model FootballZoneMVC4.DAL.Owner

<tr @if (Model != null) {
    @:data-pkey="@Model.OwnerID"
}>
    <td>
        @Html.EditorFor(model => model.OwnerNames)
        @Html.ValidationMessageFor(model => model.OwnerNames)
    </td>
    <td>
        @Html.TextAreaFor(model => model.Biography)
        @Html.ValidationMessageFor(model => model.Biography)
    </td>
    <td>
        @Html.TextAreaFor(model => model.ImageUrl)
        @Html.ValidationMessageFor(model => model.ImageUrl)
    </td>
    <td>
@if (ViewContext.RouteData.Values["Action"].ToString() == "Edit") {
        <button class="editRow">Save</button><button class="cancelEditRow">Cancel</button>
} else {
        <button class="createRow">Create</button><button class="cancelCreateRow">Cancel</button>
}
    </td>
</tr>
